<br>
<center>
<h1> HELP WANTED </h1>
<a href="/new-request" class="btn btn-primary ml-1">Make a Request</a><a href="/" class="btn btn-primary ml-1">My Assignments</a><a href="/notes" class="btn btn-primary ml-1">My Notes</a><a href="/schedule" class="btn btn-primary ml-1">Schedule</a><hr>
</center>
<div class="row">
    <div class="col-sm-12 col-sm-offset-8">
        <ul>
          {{#if requests}}
            {{#each requests}}
                <li class="list-group-item">
                    <h1>{{this.title}}</h1>
                    {{#if duedate}}
                    <h4>Due Date: {{this.duedate}}</h4>
                    {{else}}

                    {{/if}}
                    <h5>by: {{this.author.username}}</h5>
                    <br>
                    <p>{{this.description}}</p>
                    {{#if url}}
                    <a href="{{this.url}}" target="_blank">Link to Assignment</a><br><br>
                    {{else}}

                    {{/if}}
                    <a href="/helpboard/{{this._id}}" class="btn btn-primary ml-1">Read More</a>
                </li>
            {{/each}}
            {{else}}
            <h1>No Requests!</h1>
            {{/if}}
        </ul>
    </div>
</div>
